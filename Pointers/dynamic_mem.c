#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// main function
int main()
{
    // NOTE: Whenever you define a var in C, the compiler automatically
    // allocates the correct amount of storage for you based on the data type.

    return 0;
}