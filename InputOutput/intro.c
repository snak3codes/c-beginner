#include <stdio.h> // Required header file for Input Output
#include <stdlib.h>
#include <string.h>

// main function
int main()
{
    // stdio library provides functions for reading and writing to or from
    // data streams

    // NOTE: eof = END OF FILE.

    // 2 ways of writing data to a stream that represents a file
    // 1. text
    // 2. binary

    // NOTE: C programs automatically open three files on your behalf (streams)
    // standard input - the normal input device for your system, your keyboard
    // standard output - usually your display screen
    // standard error - usually your display screen

    // REASON:
    // Standard input is the file that is read by getchar(), scanf()
    // Standard output is used by putchar(), puts(), printf()
    return 0;
} //